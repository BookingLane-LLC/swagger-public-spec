{
  "openapi": "3.1.0",
  "info": {
    "title": "Get reservation status",
    "description": "Retrieve reservation status when customer provides their reservation ID. Never call without explicit user input.",
    "version": "v2.0.0"
  },
  "servers": [
    {
      "url": "https://mgr.bookinglane.com/api/ai-agent-reservations"
    }
  ],
  "paths": {
    "/status": {
      "get": {
        "description": "Retrieve reservation status ONLY after customer provides their reservation ID. Do not fabricate or guess reservation numbers.",
        "operationId": "GetReservationStatus",
        "parameters": [
          {
            "name": "reservationId",
            "in": "query",
            "description": "5-digit reservation ID that the customer must provide explicitly (never use example values like 12345)",
            "required": true,
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 10000,
              "maximum": 99999
            },
            "example": "User says: 'My reservation is 54321'"
          }
        ]
      }
    }
  }
}
